version: '3'
services:
  control-center:
    container_name: 321cqu-control-center
    build: .
    expose:
      - "53214"
    restart: always
    volumes:
      - /www/services/control_center/config:/etc/ControlCenter:ro
    networks:
      - persist

networks:
  persist:
    external:
      name: 321cqu